---
phase: 13-settings-functional-restoration
plan: 04
type: execute
wave: 2
depends_on:
  - 13-01
files_modified:
  - electron/utils/backup-manager.ts
  - electron/api/routes/backup.ts
  - electron/api/routes/memory.ts
  - src/components/settings-center/migration-backup-panel.tsx
  - src/components/settings-center/memory-strategy-panel.tsx
  - tests/unit/backup-manager.test.ts
  - tests/unit/memory-strategy.test.ts
autonomous: true
requirements:
  - SETTINGS-02
  - AGENT-03

must_haves:
  truths:
    - "User can export a .ktclaw archive containing settings, memory, and channel/provider metadata — no secrets included"
    - "User can import a .ktclaw archive and see a preview of what will be restored before committing"
    - "A backup is automatically taken before any dangerous operation (hard reset, import overwrite)"
    - "At most 10 backups are retained; oldest are pruned automatically"
    - "User can trigger manual memory snapshot, analyze, and reindex from the Memory Strategy panel"
    - "User can add/remove watched directories for auto-reindex; changes persist to main-process store"
    - "Memory Strategy panel has no placeholder text"
    - "Migration & Backup panel has no placeholder text"
  artifacts:
    - path: "electron/utils/backup-manager.ts"
      provides: "createBackup(), listBackups(), pruneBackups(), exportArchive(), importArchive()"
      exports: ["createBackup", "listBackups", "pruneBackups", "exportArchive", "importArchive", "previewImport"]
    - path: "electron/api/routes/backup.ts"
      provides: "REST routes for backup CRUD and archive import/export"
      exports: ["handleBackupRoutes"]
    - path: "electron/api/routes/memory.ts"
      provides: "Watched-directory auto-reindex watcher wired to watchedMemoryDirs setting"
      contains: "watchedMemoryDirs"
    - path: "tests/unit/backup-manager.test.ts"
      provides: "Archive round-trip, restore preview, backup retention tests"
    - path: "tests/unit/memory-strategy.test.ts"
      provides: "Watched-directory reindex trigger tests"
  key_links:
    - from: "electron/api/routes/backup.ts"
      to: "electron/utils/backup-manager.ts"
      via: "createBackup(), exportArchive(), importArchive()"
      pattern: "createBackup|exportArchive|importArchive"
    - from: "electron/utils/backup-manager.ts"
      to: "electron/utils/store.ts"
      via: "getAllSettings() for settings serialization"
      pattern: "getAllSettings|exportSettings"
    - from: "electron/api/routes/memory.ts"
      to: "electron/utils/store.ts"
      via: "getSetting('watchedMemoryDirs') to set up fs.watch"
      pattern: "watchedMemoryDirs"
---

<objective>
Implement the Migration & Backup system (.ktclaw archive format, preview-first selective restore, auto-backup before dangerous ops, 10-backup retention) and wire the Memory Strategy panel to real manual controls plus watched-directory auto-reindex.

Purpose: Both panels currently show placeholder text. Migration & Backup has no archive format, no preview, no auto-backup. Memory Strategy has no manual trigger controls and no watched-directory support.
Output: backup-manager.ts, backup REST routes, memory route extended with watched-dir watcher, real UI panels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-settings-functional-restoration/13-CONTEXT.md
@.planning/phases/13-settings-functional-restoration/13-01-SUMMARY.md
@electron/utils/store.ts
@electron/api/routes/memory.ts
@src/components/settings-center/migration-backup-panel.tsx
@src/components/settings-center/memory-strategy-panel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build backup-manager and backup REST routes</name>
  <files>electron/utils/backup-manager.ts, electron/api/routes/backup.ts, tests/unit/backup-manager.test.ts</files>
  <behavior>
    - exportArchive() produces a .ktclaw file (ZIP) containing: settings.json (from getAllSettings() minus sensitive fields), memory-snapshot.json (from /api/memory/snapshot), channel-metadata.json (provider names + channel IDs, no API keys)
    - Sensitive fields excluded from settings.json: gatewayToken, any field containing 'key', 'secret', 'token', 'password' (case-insensitive key name check)
    - previewImport(archivePath) returns { settings: object, memory: object, channelMetadata: object } without applying anything
    - importArchive(archivePath, modules: string[]) applies only the selected modules ('settings' | 'memory' | 'channelMetadata'), calls createBackup() first
    - createBackup() writes a timestamped .ktclaw file to {userData}/backups/
    - listBackups() returns array of { filename, createdAt, sizeBytes } sorted newest-first
    - pruneBackups() deletes oldest backups beyond the 10-backup limit (per D-13)
    - createBackup() calls pruneBackups() after writing
  </behavior>
  <action>
Create electron/utils/backup-manager.ts:

Use the built-in `archiver` or `adm-zip` package for ZIP creation. If neither is available, use Node's `zlib` + `tar` or implement a simple JSON-bundle format (single JSON file with base64-encoded sections). Check package.json first; prefer what is already installed.

```typescript
const BACKUP_DIR = path.join(app.getPath('userData'), 'backups');
const MAX_BACKUPS = 10;

// Sensitive field filter
const SENSITIVE_KEYS = /key|secret|token|password/i;
function sanitizeSettings(settings: AppSettings): Partial<AppSettings> {
  return Object.fromEntries(
    Object.entries(settings).filter(([k]) => !SENSITIVE_KEYS.test(k))
  ) as Partial<AppSettings>;
}
```

Implement all exported functions. For memory snapshot in exportArchive(), call the memory API internally (import the handler or read the memory files directly from the workspace — prefer direct file read to avoid circular dependency).

Create electron/api/routes/backup.ts with these routes:
- GET /api/backup/list → listBackups()
- POST /api/backup/create → createBackup(), returns { filename, createdAt }
- POST /api/backup/export → exportArchive(), streams the .ktclaw file as application/octet-stream
- POST /api/backup/preview → previewImport(body.archivePath), returns preview object
- POST /api/backup/import → importArchive(body.archivePath, body.modules), returns { success, backedUpAs }

Register handleBackupRoutes in the main API server (electron/api/server.ts or equivalent router file).

Write tests/unit/backup-manager.test.ts covering:
- exportArchive() excludes sensitive fields
- previewImport() returns correct structure without applying changes
- importArchive() calls createBackup() before applying
- pruneBackups() removes oldest when count > 10
- listBackups() returns newest-first
  </action>
  <verify>
    <automated>npx vitest run tests/unit/backup-manager.test.ts</automated>
  </verify>
  <done>All backup-manager tests pass; backup routes registered; sensitive fields excluded from archives; auto-backup fires before import</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire Memory Strategy panel and watched-directory auto-reindex</name>
  <files>electron/api/routes/memory.ts, src/components/settings-center/memory-strategy-panel.tsx, src/components/settings-center/migration-backup-panel.tsx, tests/unit/memory-strategy.test.ts</files>
  <behavior>
    - POST /api/memory/snapshot, /api/memory/analyze, /api/memory/reindex already exist; they must be callable from the UI
    - When watchedMemoryDirs changes (via settings PUT), the memory route sets up fs.watch on each directory; file changes trigger a debounced reindex (500ms debounce) for the affected agent scope
    - Removing a directory from watchedMemoryDirs tears down its watcher
    - memory-strategy-panel.tsx has no placeholder text; shows: manual snapshot button, manual analyze button, manual reindex button, watched-dirs list with add/remove
    - migration-backup-panel.tsx has no placeholder text; shows: export button, import (file picker → preview → selective restore), backup list, manual backup button
  </behavior>
  <action>
**electron/api/routes/memory.ts — watched-directory watcher:**

Add a module-level Map: `const dirWatchers = new Map<string, FSWatcher>()`.

Export a function `syncWatchedDirs(dirs: string[]): void` that:
1. Tears down watchers for directories no longer in `dirs`.
2. Sets up `fs.watch(dir, { recursive: true }, debounce(() => triggerReindex(dir), 500))` for new directories.
3. `triggerReindex(dir)` calls the existing reindex logic for all agent scopes whose workspaceDir is under `dir`.

In electron/api/routes/settings.ts (already has side-effect hooks from Plan 01), add: when `watchedMemoryDirs` changes, call `syncWatchedDirs(newValue)`.

**src/components/settings-center/memory-strategy-panel.tsx:**

Remove all placeholder text. Implement:
- Three buttons: "Snapshot", "Analyze", "Reindex" — each calls the corresponding POST /api/memory/* endpoint via hostApiFetch and shows a loading state + success/error toast.
- Watched Directories section: list of `watchedMemoryDirs` from settings store, with an "Add Directory" button (opens a native directory picker via IPC or a text input), and a remove (×) button per entry. Changes call `setWatchedMemoryDirs(newList)` from the store.

**src/components/settings-center/migration-backup-panel.tsx:**

Remove all placeholder text. Implement:
- "Export Backup" button: calls POST /api/backup/export, triggers file save dialog.
- "Import" button: opens file picker for .ktclaw files, calls POST /api/backup/preview, shows a preview modal with checkboxes for each module (settings / memory / channelMetadata), then calls POST /api/backup/import with selected modules on confirm (per D-09).
- "Create Backup Now" button: calls POST /api/backup/create.
- Backup list: calls GET /api/backup/list on mount, shows filename + date + size.

Write tests/unit/memory-strategy.test.ts covering:
- syncWatchedDirs(['/a']) sets up a watcher for /a
- syncWatchedDirs([]) tears down the /a watcher
- File change in watched dir triggers debounced reindex call
  </action>
  <verify>
    <automated>npx vitest run tests/unit/memory-strategy.test.ts</automated>
  </verify>
  <done>Memory strategy tests pass; memory-strategy-panel.tsx has no placeholder text; migration-backup-panel.tsx has no placeholder text; watched-dir watcher is set up/torn down correctly; TypeScript compiles clean</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/backup-manager.test.ts tests/unit/memory-strategy.test.ts` passes
- `npx tsc --noEmit` reports no new errors
- No "not yet implemented" or "placeholder" text in migration-backup-panel.tsx or memory-strategy-panel.tsx
- GET /api/backup/list returns an array (may be empty on fresh install)
- Sensitive fields (gatewayToken, API keys) are absent from exported .ktclaw archives
</verification>

<success_criteria>
.ktclaw archive export/import works with preview-first selective restore. Auto-backup fires before dangerous operations. 10-backup retention is enforced. Memory Strategy panel exposes real manual controls and watched-directory auto-reindex. Both panels have no placeholder text. SETTINGS-02 and AGENT-03 requirements are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/13-settings-functional-restoration/13-04-SUMMARY.md`
</output>
