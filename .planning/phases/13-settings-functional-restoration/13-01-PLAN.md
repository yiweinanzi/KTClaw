---
phase: 13-settings-functional-restoration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/utils/store.ts
  - electron/api/routes/settings.ts
  - src/stores/settings.ts
  - tests/unit/settings-persistence.test.ts
autonomous: true
requirements:
  - SETTINGS-01
  - SETTINGS-02
  - AGENT-03

must_haves:
  truths:
    - "All settings fields (ACL, identity, memory strategy, desktop behavior) survive a round-trip through the host API and are readable back from the main-process store"
    - "Renderer settings mutations go through hostApiFetch('/api/settings') — no direct localStorage-only writes for persisted fields"
    - "AppSettings interface in store.ts contains every field the renderer SettingsState declares"
  artifacts:
    - path: "electron/utils/store.ts"
      provides: "Extended AppSettings interface with ACL, identity, memory strategy, desktop behavior fields"
      contains: "globalRiskLevel, fileAcl, terminalAcl, networkAcl, minimizeToTray, startMinimized, brandSubtitle, myName, watchedMemoryDirs, notificationsEnabled"
    - path: "electron/api/routes/settings.ts"
      provides: "Side-effect handlers for new fields (minimizeToTray, startMinimized, notifications)"
      exports: ["handleSettingsRoutes"]
    - path: "src/stores/settings.ts"
      provides: "Renderer store wired to hostApiFetch for all persisted mutations"
      contains: "init() loads from /api/settings, mutations call PUT /api/settings"
    - path: "tests/unit/settings-persistence.test.ts"
      provides: "Round-trip coverage for all new fields"
  key_links:
    - from: "src/stores/settings.ts"
      to: "electron/api/routes/settings.ts"
      via: "hostApiFetch PUT /api/settings"
      pattern: "hostApiFetch.*api/settings"
    - from: "electron/api/routes/settings.ts"
      to: "electron/utils/store.ts"
      via: "setSetting / getAllSettings"
      pattern: "setSetting|getAllSettings"
---

<objective>
Extend the main-process AppSettings schema to cover every field the renderer SettingsState already declares but that currently has no main-process persistence. Wire the renderer store so all mutations flow through hostApiFetch rather than staying renderer-only.

Purpose: Every subsequent plan in Phase 13 depends on settings being persisted to disk. Without this spine, Tool Permissions, Desktop Behavior, Identity, and Memory Strategy changes evaporate on restart.
Output: Extended store.ts schema, updated settings route with side-effect hooks, renderer store wired to host API, round-trip tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-settings-functional-restoration/13-CONTEXT.md
@electron/utils/store.ts
@electron/api/routes/settings.ts
@src/stores/settings.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend AppSettings schema and add side-effect hooks</name>
  <files>electron/utils/store.ts, electron/api/routes/settings.ts</files>
  <behavior>
    - getAllSettings() returns an object that includes globalRiskLevel, fileAcl, terminalAcl, networkAcl, minimizeToTray, startMinimized, brandSubtitle, myName, watchedMemoryDirs (string[]), notificationsEnabled (boolean)
    - setSetting('globalRiskLevel', 'strict') persists and is readable back via getSetting
    - setSetting('minimizeToTray', true) persists correctly
    - setSetting('brandSubtitle', 'My Subtitle') persists correctly
    - setSetting('watchedMemoryDirs', ['/home/user/docs']) persists as array
  </behavior>
  <action>
In electron/utils/store.ts, add the following fields to the AppSettings interface (they are already in the renderer SettingsState but absent from AppSettings):

```
// Tool Permissions
globalRiskLevel: 'standard' | 'strict' | 'permissive'
fileAcl: boolean
terminalAcl: boolean
networkAcl: boolean
channelRouteRules: string[]
filePathAllowlist: string[]
terminalCommandBlocklist: string[]
customToolGrants: string[]

// Desktop Behavior
minimizeToTray: boolean

// Identity
brandSubtitle: string
myName: string

// Memory Strategy
watchedMemoryDirs: string[]
notificationsEnabled: boolean
```

Add defaults for each new field in the getSettingsStore() initialization block (globalRiskLevel: 'standard', fileAcl: true, terminalAcl: true, networkAcl: true, minimizeToTray: false, brandSubtitle: '', myName: '', watchedMemoryDirs: [], notificationsEnabled: true, channelRouteRules: [], filePathAllowlist: [], terminalCommandBlocklist: [], customToolGrants: []).

In electron/api/routes/settings.ts, add two new patch-detection helpers:
- patchTouchesMinimizeToTray(patch): returns true if patch contains 'minimizeToTray'
- patchTouchesNotifications(patch): returns true if patch contains 'notificationsEnabled'

In the PUT /api/settings handler, after the existing proxy/launchAtStartup/gatewayPort side-effect checks, add:
- if patchTouchesMinimizeToTray(patch): emit 'settings:minimizeToTray-changed' on ctx.eventBus (or store for later consumption by index.ts)
- if patchTouchesNotifications(patch): emit 'settings:notifications-changed' on ctx.eventBus

Do NOT wire the actual window behavior here — that is Plan 02's job. Just ensure the side-effect hooks exist and the fields persist.
  </action>
  <verify>
    <automated>npx vitest run tests/unit/settings-persistence.test.ts</automated>
  </verify>
  <done>AppSettings interface contains all new fields with defaults; round-trip tests pass for all new fields; settings route emits side-effect events for minimizeToTray and notificationsEnabled changes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire renderer store to host API for all persisted mutations</name>
  <files>src/stores/settings.ts, tests/unit/settings-persistence.test.ts</files>
  <behavior>
    - init() calls GET /api/settings and merges the response into store state
    - setGlobalRiskLevel('strict') calls PUT /api/settings with { globalRiskLevel: 'strict' }
    - setMinimizeToTray(true) calls PUT /api/settings with { minimizeToTray: true }
    - setBrandSubtitle('x') calls PUT /api/settings with { brandSubtitle: 'x' }
    - setMyName('Alice') calls PUT /api/settings with { myName: 'Alice' }
    - setWatchedMemoryDirs(['/a']) calls PUT /api/settings with { watchedMemoryDirs: ['/a'] }
    - setNotificationsEnabled(false) calls PUT /api/settings with { notificationsEnabled: false }
    - Fields that are already wired (theme, language, launchAtStartup, proxy fields, gatewayPort) must not regress
  </behavior>
  <action>
In src/stores/settings.ts:

1. Add the missing fields to SettingsState that are in AppSettings but not yet in the renderer interface: watchedMemoryDirs (string[]), notificationsEnabled (boolean). Keep all existing fields.

2. Add action signatures to SettingsState:
   - setGlobalRiskLevel(level: GlobalRiskLevel): void
   - setFileAcl(v: boolean): void
   - setTerminalAcl(v: boolean): void
   - setNetworkAcl(v: boolean): void
   - setMinimizeToTray(v: boolean): void
   - setBrandSubtitle(v: string): void
   - setMyName(v: string): void
   - setWatchedMemoryDirs(dirs: string[]): void
   - setNotificationsEnabled(v: boolean): void

3. Implement each action as: set the local state AND call hostApiFetch('/api/settings', { method: 'PUT', body: JSON.stringify({ [field]: value }) }). Fire-and-forget is acceptable (no await needed in the setter, but log errors).

4. In init(), after loading from /api/settings, merge ALL fields from the API response into the store state (not just the subset currently merged). This ensures main-process values win on startup.

5. Write tests/unit/settings-persistence.test.ts covering:
   - init() merges API response fields
   - Each new setter calls PUT /api/settings with the correct payload
   - Existing setters (setTheme, setLanguage) still call PUT /api/settings (regression guard)
  </action>
  <verify>
    <automated>npx vitest run tests/unit/settings-persistence.test.ts</automated>
  </verify>
  <done>All new setters call PUT /api/settings; init() merges all fields from API response; test file passes with no skipped tests</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/unit/settings-persistence.test.ts` passes
- `npx tsc --noEmit` reports no new type errors in electron/utils/store.ts or src/stores/settings.ts
- AppSettings interface field count matches SettingsState field count (excluding action functions)
</verification>

<success_criteria>
All settings fields that Phase 13 plans depend on (ACL, identity, memory strategy, desktop behavior) are persisted to the main-process store and readable back. The renderer store is the single mutation point and always calls the host API. No existing settings behavior regresses.
</success_criteria>

<output>
After completion, create `.planning/phases/13-settings-functional-restoration/13-01-SUMMARY.md`
</output>
