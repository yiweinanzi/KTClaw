目前的静态 UI 质感非常棒，完全符合极简且现代的设计要求！但目前只完成了“形”，还缺少“神”。

当前问题：
当我从右侧面板拖拽 Agent 卡片到中间的「还没有团队」区域并松手时，没有任何反应。卡片回弹，没有触发任何操作。

下一步任务：请打通「从 0 到 1」的拖拽创建交互闭环

请参考 03-CONTEXT.md 中的设计决策，重点实现以下逻辑：

1. 激活 Dropzone 与拖拽视觉反馈 (D-13)

请将中间这个「还没有团队」的大卡片区域，甚至整个左侧主内容区，注册为合法的拖拽接收区（Dropzone）。

关键交互：当用户拖拽着 Agent 进入该区域上方（DragOver）时，左侧区域必须有明显的视觉高亮反馈（例如背景色变为极浅的蓝色、边框变为高亮的虚线或发光效果），明确告诉用户“这里可以松手”。

2. 实现 Drop 后的状态流转 (D-20, D-11, D-12)

当用户将 Agent 扔进（Drop）该区域后，必须立即隐藏「还没有团队」的空状态引导。

此时，UI 应该平滑过渡（最好使用 framer-motion）到**「团队创建模式」**。

在创建模式下，应该明确渲染出 Leader 区和成员区：

刚刚拖入的第一个 Agent，自动落入并占据 Leader 区。

下方显示空的成员区（带虚线框，提示“建议再拖入 2-3 名成员”）。

3. 呼出创建确认表单 (D-14, D-15)

伴随上述状态的切换，区域下方需要展开一个确认表单。

团队名称：自动填充为“【Leader 名称】的团队”（例如：Main 的团队），并且允许用户直接在输入框内修改。

包含「确认创建」和「取消」按钮。点击取消则清空状态，退回至“空状态引导”页面。

请检查目前项目中使用的拖拽库（无论是 HTML5 API 还是 dnd-kit 等），确保拖拽时的跟随卡片（Drag Overlay）保持一定的半透明度，且拖入后的状态切换足够流畅。