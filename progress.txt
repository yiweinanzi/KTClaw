# Ralph Progress Log
Started: 2026-03-26T00:00:00.000Z
---

## Codebase Patterns
- Renderer business logic should call backend work through `hostApiFetch<T>()` or `invokeIpc`, not direct backend `fetch`.
- Zustand stores live under `src/stores/` and new UI state should follow existing store patterns instead of ad-hoc globals.
- Scope for this Ralph run explicitly excludes i18n cleanup, translation parity work, and disabled Docs or Help restoration.
- Do not reintroduce the removed Chat top-level `Export` button.
- Preserve existing unrelated workspace changes and only advance the remaining non-i18n business backlog from `Prompt.md`.

## [2026-03-26 00:58:36 +08:00] - US-001
- Implemented persisted `executionRecords` for runtime subagent sessions by deriving normalized tool execution summaries from structured history, saving them with session records, and hydrating older saved records from `history` when the new field is absent.
- Files changed: `electron/services/session-runtime-manager.ts`, `tests/unit/session-runtime-manager.test.ts`, `src/pages/Cron/index.tsx`, `src/pages/TaskKanban/index.tsx`, `AGENTS.md`, `prd.json`, `progress.txt`
- Learnings for future iterations:
  - Reusable patterns: derive runtime drill-down summaries from persisted structured `history` so newer fields can stay backward compatible with older saved records.
  - Gotchas: this workspace can already contain unrelated dirty UI changes; if `tsc` fails in those files, stage only the exact compile-fix hunks needed for the current story.
  - Useful context: runtime session persistence now carries `transcript`, structured `history`, and summarized `executionRecords`, which should be the baseline for later runtime tree drill-down work.
---
